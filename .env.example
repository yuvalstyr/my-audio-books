# Environment Configuration for Railway Deployment

# Node environment
NODE_ENV=production

# Database configuration
# Railway will automatically set RAILWAY_VOLUME_MOUNT_PATH when using volumes
# DATABASE_PATH=/app/data/audiobook-wishlist.db

# Optional: Custom database path (if not using Railway volumes)
# DATABASE_PATH=./prod.db

# Application configuration
# PORT=3000  # Railway sets this automatically

# Optional: Enable debug logging
# DEBUG=true